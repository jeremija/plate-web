define(['globalize', '../locale/globalize.culture.en-US'],
    function(Globalize, a) {

    Globalize.addCultureInfo('en-US', {
        messages: {
            'test': 'test en-US'
        }
    });
});